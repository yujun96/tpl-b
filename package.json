{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "yj-cli": "index.js"
  },
  "author": "{{author}}",
  "license": "ISC",
  "dependencies": {
    "commander": "^6.1.0",
    "download-git-repo": "^3.0.2"
  }
}
